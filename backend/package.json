{
  "name": "@pulsechain/backend",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:core": "tsx --env-file=../.env tests/run-acceptance.ts",
    "test:requisitions": "tsx --env-file=../.env tests/run-requisition-tests.ts",
    "test:fulfillment": "tsx --env-file=../.env tests/run-fulfillment-tests.ts",
    "test:workflow": "tsx --env-file=../.env tests/run-workflow-tests.ts"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.645.0",
    "@aws-sdk/client-ses": "^3.1135.0",
    "@aws-sdk/client-sfn": "^3.1135.0",
    "@aws-sdk/lib-dynamodb": "^3.645.0",
    "@pulsechain/seed": "*",
    "@pulsechain/shared": "*"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.143",
    "@types/node": "^22.5.5",
    "esbuild": "^0.23.1",
    "typescript": "^5.5.4"
  }
}
